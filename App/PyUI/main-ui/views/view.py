

from abc import ABC


class View(ABC):
    def __init__(self):
        pass

    def view_finished():
        #Callers should always call this one the view
        #is done with so it can be cleaned up as needed
        pass